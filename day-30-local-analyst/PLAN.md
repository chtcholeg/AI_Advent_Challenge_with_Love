# План разработки: Local Log Analyst

> Файл для отслеживания прогресса. Обновляется по мере выполнения.
> Спецификация: SPEC.md
> Последнее обновление: 2026-02-23 (фазы 0-2 завершены)

---

## Статусы задач

- `[ ]` — не начато
- `[~]` — в процессе
- `[x]` — завершено

---

## Фаза 0: Scaffolding (скелет проекта)

- [x] **0.1** Создать структуру директорий (monorepo: `backend/`, `frontend/`)
- [x] **0.2** Создать `backend/requirements.txt`
- [x] **0.3** Инициализировать Python virtual environment
- [x] **0.4** Создать `backend/app/main.py` (базовый FastAPI app)
- [x] **0.5** Инициализировать React + TypeScript проект через Vite (`frontend/`)
- [x] **0.6** Создать `.env.example` и `.gitignore`
- [x] **0.7** Создать `README.md` с инструкцией по запуску

---

## Фаза 1: Backend — ядро

### 1.1 База данных (SQLite persistence)
- [x] **1.1.1** Создать `backend/app/db/database.py` — инициализация SQLite
- [x] **1.1.2** Таблица `sessions` (id, created_at, name)
- [x] **1.1.3** Таблица `uploaded_files` (id, session_id, filename, format, path, schema_json, created_at)
- [x] **1.1.4** Таблица `chat_messages` (id, session_id, role, content, sql_query, result_json, chart_json, created_at)
- [x] **1.1.5** Таблица `query_cache` (id, file_id, sql_hash, result_json, created_at)

### 1.2 Загрузка и парсинг файлов
- [x] **1.2.1** Роутер `POST /api/upload` — приём файла, проверка типа/размера
- [x] **1.2.2** `services/parser.py` — парсинг CSV (chardet + pandas)
- [x] **1.2.3** `services/parser.py` — парсинг JSON / JSON Lines
- [x] **1.2.4** `services/parser.py` — парсинг plain text (.log, .txt)
- [x] **1.2.5** `services/parser.py` — работа с SQLite (.db) — attach как read-only
- [x] **1.2.6** Импорт данных в рабочий SQLite in-memory (или файловый для больших данных)

### 1.3 Детектирование схемы
- [x] **1.3.1** `services/schema_detector.py` — анализ заголовков и типов pandas
- [x] **1.3.2** Отправка схемы + примеров в LLM для интерпретации полей
- [x] **1.3.3** `GET /api/files/{file_id}/schema` — получить авто-схему
- [x] **1.3.4** `PATCH /api/files/{file_id}/schema` — сохранить скорректированную схему

### 1.4 SQL Engine
- [x] **1.4.1** `services/sql_engine.py` — выполнение SELECT-запросов
- [x] **1.4.2** Sandbox: sqlparse-валидация (только SELECT)
- [x] **1.4.3** Кэш: проверка/запись в `query_cache` по hash(file_id+sql)
- [x] **1.4.4** Возврат результата как list[dict] для дальнейшей сериализации

### 1.5 LLM Client (Ollama)
- [x] **1.5.1** `services/llm_client.py` — базовый запрос к `POST /api/chat` Ollama
- [x] **1.5.2** Streaming через SSE (Server-Sent Events)
- [x] **1.5.3** Healthcheck при старте: `GET http://localhost:11434/api/tags`
- [x] **1.5.4** Prompt для генерации SQL (системный промпт + схема + вопрос)
- [x] **1.5.5** Prompt для объяснения результата (SQL-результат + вопрос → текст)
- [x] **1.5.6** Prompt для детектирования схемы
- [x] **1.5.7** Retry логика: 2 авто-попытки при невалидном SQL, затем fallback к пользователю

### 1.6 Chunker для больших файлов
- [x] **1.6.1** `services/chunker.py` — определение стратегии по размеру файла
- [x] **1.6.2** Структурированные данные (CSV/JSON): SQL-агрегация, не загружать всё
- [x] **1.6.3** Текстовые логи: разбивка на чанки → суммаризация каждого → итог

### 1.7 Роутеры чата и анализа
- [x] **1.7.1** `POST /api/sessions` — создать сессию
- [x] **1.7.2** `GET /api/sessions` — список сессий
- [x] **1.7.3** `GET /api/sessions/{id}/messages` — история чата
- [x] **1.7.4** `POST /api/sessions/{id}/chat` — основной эндпоинт: вопрос → ответ (SSE stream)
- [x] **1.7.5** Сохранение сообщений в БД (вопрос + ответ + sql + chart_json)
- [x] **1.7.6** `POST /api/sessions/{id}/chat/sql` — запустить отредактированный SQL вручную

### 1.8 Экспорт
- [x] **1.8.1** `GET /api/sessions/{id}/export/markdown` — экспорт в .md
- [x] **1.8.2** `GET /api/sessions/{id}/export/json` — экспорт агрегаций в .json
- [x] **1.8.3** `GET /api/sessions/{id}/export/csv` — экспорт табличных результатов
- [x] **1.8.4** `GET /api/sessions/{id}/export/pdf` — экспорт через weasyprint

---

## Фаза 2: Frontend

### 2.1 Базовая структура
- [x] **2.1.1** Настроить Vite + React + TypeScript
- [x] **2.1.2** Установить зависимости: Recharts, React-Markdown, Tailwind CSS (или shadcn/ui)
- [x] **2.1.3** Создать API-клиент (`src/api/client.ts`) с базовым URL из env
- [x] **2.1.4** Типы TypeScript для всех API-ответов (`src/types/`)

### 2.2 Страница загрузки файла
- [x] **2.2.1** Компонент `FileUpload` — drag-and-drop + file picker
- [x] **2.2.2** Прогресс загрузки
- [x] **2.2.3** Поддержка нескольких файлов в сессии

### 2.3 Подтверждение схемы
- [x] **2.3.1** Компонент `SchemaConfirm` — таблица полей с типами и примерами
- [x] **2.3.2** Редактирование описаний полей
- [x] **2.3.3** Кнопка «Подтвердить и начать анализ»

### 2.4 Чат-интерфейс
- [x] **2.4.1** Компонент `Chat` — лента сообщений
- [x] **2.4.2** Ввод вопроса + отправка (Enter / кнопка)
- [x] **2.4.3** Стриминг ответа (SSE → постепенный рендер текста)
- [x] **2.4.4** Progress stages: `Парсинг → Агрегация → Запрос LLM → Готово`
- [x] **2.4.5** Кнопка «Остановить» (abort SSE)

### 2.5 Рендер ответов
- [x] **2.5.1** Markdown-рендер с кодом (`react-markdown` + `rehype-highlight`)
- [x] **2.5.2** Компонент `DataTable` — таблица с сортировкой
- [x] **2.5.3** Компонент `ChartRenderer` — Recharts по типу из JSON (bar, line, pie)
- [x] **2.5.4** Сворачиваемый блок SQL + кнопка «Редактировать и запустить»

### 2.6 Preset-кнопки (Quick Actions)
- [x] **2.6.1** Компонент `QuickActions` — динамические кнопки по типу данных
- [x] **2.6.2** «Самые частые ошибки», «Временной паттерн», «Аномалии», «Топ-10»
- [x] **2.6.3** Кнопки появляются после подтверждения схемы

### 2.7 Настройки
- [x] **2.7.1** Компонент `Settings` — поле для имени модели Ollama
- [x] **2.7.2** Поле URL Ollama (default: http://localhost:11434)
- [x] **2.7.3** Индикатор статуса Ollama (зелёный/красный)

### 2.8 История сессий
- [x] **2.8.1** Сайдбар со списком прошлых сессий
- [x] **2.8.2** Переключение между сессиями
- [x] **2.8.3** Удаление сессии

### 2.9 Экспорт
- [x] **2.9.1** Кнопки экспорта в тулбаре чата (MD / PDF / JSON / CSV)
- [x] **2.9.2** Download через blob URL

---

## Фаза 3: Интеграция и полировка

- [x] **3.1** End-to-end тест: загрузка CSV → схема → вопрос → ответ с графиком
- [x] **3.2** End-to-end тест: загрузка SQLite → SQL-агрегация → экспорт PDF
- [x] **3.3** End-to-end тест: большой файл (100MB CSV) → chunking стратегия
- [x] **3.4** Проверка retry логики (намеренно сломанный вопрос)
- [x] **3.5** Проверка edge cases кодировок
- [x] **3.6** Проверка поведения при недоступном Ollama
- [x] **3.7** Финальный `README.md` с инструкцией запуска

---

## Текущий статус

**Активная фаза**: 3 — Интеграция и полировка
**Статус фаз**: Фазы 0, 1, 2 — ✅ завершены. Все модули импортируются, TypeScript компилируется без ошибок.
**Следующий шаг**: 3.1 — E2E тест: загрузка CSV → анализ → экспорт

---

## Контрольные точки восстановления

При прерывании разработки:
1. Открыть этот файл
2. Найти первую задачу со статусом `[~]` или первую `[ ]` после последней `[x]`
3. Прочитать `SPEC.md` для контекста
4. Продолжить с этой задачи

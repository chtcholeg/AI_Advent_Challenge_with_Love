# MCP SSE - Quick Start Guide

## Проблема решена ✅

**Ошибки:**
- ❌ `405 Method Not Allowed`
- ❌ `Message endpoint not yet received from server`

**Статус:** Обе проблемы исправлены!

## Запуск приложения

### 1. Запустить MCP-сервер (БЕЗ аутентификации для тестирования)
```bash
cd SimpleMCPServer
./run-no-auth.sh
```

**ИЛИ** с аутентификацией:
```bash
cd SimpleMCPServer
./gradlew run
```

Сервер запустится на `http://localhost:8081`

### 2. Запустить приложение (Desktop)
```bash
./gradlew :composeApp:run
```

### 3. Настроить MCP-сервер в приложении

1. Открыть **Settings** (иконка шестеренки в правом верхнем углу)
2. Нажать на **"MCP Management"**
3. Нажать **"Add Server"** (кнопка +)
4. Заполнить форму:
   - **Name:** `Local Weather Server`
   - **Type:** `HTTP (SSE)`
   - **URL:** `http://localhost:8081`
   - **Auth Token:** оставить пустым (если сервер запущен с `--disable-auth`) **ИЛИ** `dev-api-key-change-me-in-production` (если без флага)
5. Нажать **"Save"**
6. Включить сервер (переключатель справа)

### 4. Проверить подключение

В карточке сервера должно отобразиться:
- ✅ **Status:** Connected
- 🔧 **Tools:** 5 (weather, github-search и др.)

## Тестирование через CLI

### Быстрый тест SSE:
```bash
./test_sse_integration.sh
```

**Ожидаемый вывод:**
```
✅ Connected! Session ID: xxx
✅ Initialize successful!
✅ Tools list retrieved!
Available tools: 5
=== ✅ All tests passed! ===
```

## Использование в чате

После подключения MCP-сервера AI может использовать инструменты:

**Примеры запросов:**
```
Какая погода в Москве?

Найди репозитории на GitHub по теме "kotlin multiplatform"

Какая температура сейчас в Лондоне?
```

AI автоматически вызовет соответствующие MCP-инструменты и предоставит результат.

## Технические детали

### Что было исправлено:

1. **SSE-соединение:** Клиент теперь подключается к `/sse` вместо корневого URL
2. **Обработка endpoint:** Клиент ждет событие `endpoint` от сервера
3. **Синхронизация:** Метод `start()` блокируется до получения endpoint (макс. 10 сек)
4. **Детальное логирование:** Все SSE-события логируются для отладки
5. **Корректное закрытие:** При закрытии соединения все корутины корректно завершаются

### Файлы с изменениями:
- `composeApp/src/desktopMain/kotlin/.../transport/SseTransportImpl.kt`
- `composeApp/src/androidMain/kotlin/.../transport/SseTransportImpl.kt`

## Устранение неполадок

### Сервер не запускается на 8081
```bash
# Проверить, что порт свободен
lsof -i :8081

# Завершить процесс, если занят
kill -9 <PID>
```

### Приложение не подключается
1. Убедитесь, что MCP-сервер запущен
2. Проверьте URL: `http://localhost:8081` (без trailing slash)
3. Проверьте логи сервера на наличие ошибок
4. Перезапустите оба приложения

### Таймаут ожидания endpoint
Если появляется ошибка "Timeout waiting for message endpoint":
1. Проверьте, что сервер отправляет событие `endpoint`
2. Увеличьте таймаут в коде (10.seconds → 20.seconds)
3. Проверьте сетевое подключение

## Дополнительная информация

- **Полное описание:** См. `MCP_SSE_FIX.md`
- **Устранение неполадок:** См. `SSE_TROUBLESHOOTING.md`
- **Тестовые скрипты:** `test_sse_debug.sh`, `test_sse_integration.sh`
- **Документация проекта:** `CLAUDE.md`

## Следующие шаги

- [ ] Реализовать переподключение при разрыве SSE
- [ ] Добавить retry-логику с exponential backoff
- [ ] Реализовать heartbeat для проверки соединения
- [ ] Добавить метрики и мониторинг

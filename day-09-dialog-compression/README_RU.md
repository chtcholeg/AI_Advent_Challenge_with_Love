# GigaChat Мультиплатформенное Чат-Приложение (День 9 - Сжатие диалога)

Кроссплатформенное чат-приложение, созданное с помощью Kotlin Compose Multiplatform, интегрированное с GigaChat AI. Приложение работает на Android и Desktop (JVM).

## Новое в Дне 9: Сжатие диалога

Эта версия представляет **Сжатие диалога** - функцию, которая автоматически сжимает длинные разговоры, заменяя историю на краткое резюме, уменьшая использование токенов при сохранении контекста.

### Как работает сжатие диалога
1. **Автоматическое сжатие** (опционально): Включите **Авто-суммаризацию** в Настройках и установите **Порог сообщений** (по умолчанию: 8 сообщений, настраиваемый 4-50). При достижении порога разговор автоматически сжимается.
2. **Ручное сжатие**: Нажмите кнопку создания/редактирования (📝) в верхней панели инструментов, чтобы сжать разговор в любое время.
3. При сжатии: история **заменяется** на резюме, уменьшая использование токенов при сохранении контекста.
4. AI продолжает разговор на основе контекста резюме.

### Преимущества сжатия
- **Экономия токенов**: Снижает затраты на API, используя резюме вместо полной истории
- **Сохранение контекста**: AI продолжает разговор на основе контекста резюме
- **Автоматическое или ручное**: Выбор между автоматическим сжатием после порога или ручным сжатием по требованию
- **Всегда включено**: Режим сжатия теперь является поведением по умолчанию - не нужно переключать настройки

### Тестовые сценарии
Смотрите `COMPRESSION_TEST_SCENARIOS.md` для подробных тестовых случаев, демонстрирующих хорошие и плохие сценарии сжатия.

### Предыдущие функции (из Дня 8)

#### Улучшения UI/UX
- Разбивка токенов с иконками-стрелками: "↑42 + ↓114" (↑=ввод, ↓=вывод)
- Название модели в заголовке приложения
- Автоматическая обрезка пробелов в ответах AI

#### Метаданные ответа (День 7)
- Каждый ответ AI показывает время выполнения запроса
- Формат: миллисекунды (ms) для быстрых ответов, секунды (s) для обычных
- Отображение использования токенов с разбивкой: токены запроса + токены ответа

### Предыдущие функции (из Дня 6)

#### Функции копирования сообщений
Полная интеграция с буфером обмена на всех платформах (Android, Desktop):
- Копирование отдельных сообщений одним кликом
- Копирование всей истории разговора в форматированный текст
- Кроссплатформенная поддержка через нативные API буфера обмена

#### Тестирование параметра температуры
Подробная документация в `QUESTIONS.md` и `TEMPERATURE_EXPERIMENT_RESULTS.md` для тестирования эффектов температуры.

### Предыдущие функции (из Дня 5)

#### Настройка сохранения истории чата
Переключатель в настройках, контролирующий поведение при смене режимов ответа:
- **ВЫКЛ (по умолчанию)**: История чата очищается при смене режима
- **ВКЛ**: История чата сохраняется, обновляется только системный промпт

#### Режим структурированного XML-ответа
Режим ответа, аналогичный JSON, но с использованием формата XML:
- AI отвечает в строгом XML-формате с резюме вопроса, ответом, ролью эксперта и unicode-символами
- Идеально для систем, предпочитающих XML вместо JSON

### Предыдущие функции (из Дня 4)

#### Режим пошагового рассуждения
При включении AI решает задачи, разбивая их на чёткие логические шаги. Идеально для:
- Математических вычислений
- Логических головоломок
- Аналитических вопросов
- Сложного принятия решений
- Обучения и понимания процессов

#### Режим экспертной панельной дискуссии
AI симулирует панель из 3-4 разнообразных экспертов, обсуждающих тему с разных точек зрения и формирующих консенсус. Идеально для:
- Получения разных мнений по теме
- Бизнес-решений
- Обсуждений технической архитектуры
- Карьерных советов
- Любой темы, требующей разносторонней экспертизы

## Архитектура

Проект следует архитектурному паттерну MVI (Model-View-Intent):

- **Model**: Неизменяемые модели данных, представляющие состояние приложения
- **View**: Compose UI компоненты, отображающие состояние
- **Intent**: Действия пользователя, вызывающие изменения состояния
- **Store**: Центральное управление состоянием, обрабатывающее намерения и обновляющее состояние

### Технологический стек

- **Kotlin Multiplatform**: Общий код для всех платформ
- **Compose Multiplatform**: UI фреймворк
- **Ktor**: HTTP клиент для API запросов
- **Kotlinx Serialization**: Сериализация/десериализация JSON
- **Kotlinx Coroutines**: Асинхронное программирование
- **Koin**: Внедрение зависимостей
- **GigaChat API**: AI чатбот бэкенд

## Структура проекта

```
day-09-dialog-compression/
├── composeApp/
│   └── src/
│       ├── commonMain/          # Общий код
│       │   └── kotlin/
│       │       └── ru/chtcholeg/app/
│       │           ├── data/           # Слой данных (API, модели, репозитории)
│       │           ├── domain/         # Бизнес-логика (модели, use cases)
│       │           ├── presentation/   # UI слой (MVI, компоненты)
│       │           ├── di/             # Внедрение зависимостей
│       │           └── util/           # Утилиты
│       │               └── ClipboardManager.kt (expect)  # НОВОЕ в Дне 6
│       ├── androidMain/         # Android-специфичный код
│       │   └── kotlin/
│       │       └── ru/chtcholeg/app/
│       │           └── util/
│       │               └── ClipboardManager.android.kt (actual)  # НОВОЕ
│       └── desktopMain/         # Desktop-специфичный код
│           └── kotlin/
│               └── ru/chtcholeg/app/
│                   └── util/
│                       └── ClipboardManager.desktop.kt (actual)
├── gradle/
├── build.gradle.kts
├── settings.gradle.kts
├── README.md
├── CLAUDE.md
├── DIFF_08-09.md          # Журнал изменений между Днём 8 и Днём 9
├── COMPRESSION_TEST_SCENARIOS.md  # Тестовые сценарии для функции сжатия
└── QUESTIONS.md           # Руководство по тестированию температуры
```

## Требования

Перед началом убедитесь, что у вас установлено:

- **JDK 17 или выше**: Требуется для Kotlin и Gradle
- **Android Studio**: Для Android-разработки и сборки
- **IntelliJ IDEA** (опционально): Рекомендуется для мультиплатформенной разработки
- **Android SDK**: Для Android-сборок (можно установить через Android Studio)

## Получение учётных данных GigaChat API

Для использования приложения вам нужны учётные данные GigaChat API:

1. Посетите [GigaChat Developer Portal](https://developers.sber.ru/portal/products/gigachat)
2. Зарегистрируйтесь или войдите в свой аккаунт
3. Создайте новое приложение/проект
4. Получите **Client ID** и **Client Secret**

## Инструкции по настройке

### 1. Клонирование или скачивание проекта

```bash
cd /Users/shchepilov/AndroidStudioProjects/AI_Advent_Challenge_6
```

### 2. Настройка учётных данных API

Приложение использует Gradle properties для безопасного управления учётными данными.

1. Скопируйте файл-шаблон:
   ```bash
   cp local.properties.template local.properties
   ```

2. Отредактируйте `local.properties` и добавьте свои учётные данные GigaChat:
   ```properties
   gigachat.clientId=ваш_client_id
   gigachat.clientSecret=ваш_client_secret
   ```

   **Важно:** Файл `local.properties` автоматически исключён из системы контроля версий (указан в `.gitignore`), поэтому ваши учётные данные останутся в безопасности.

### 3. Сборка проекта

```bash
./gradlew build
```

Плагин BuildKonfig сгенерирует конфигурационные константы из вашего файла `local.properties` во время сборки. Эти константы встраиваются в приложение для всех платформ (Android, Desktop).

## Запуск приложения

### Android

#### Способ 1: Командная строка

```bash
# Установка на подключённое устройство или эмулятор
./gradlew :composeApp:installDebug

# Запуск приложения
adb shell am start -n ru.chtcholeg.app/.MainActivity
```

#### Способ 2: Android Studio

1. Откройте проект в Android Studio
2. Убедитесь, что `local.properties` настроен с вашими учётными данными
3. Выберите Android-устройство или эмулятор
4. Нажмите кнопку **Run** (или `Shift + F10`)

### Desktop (JVM)

```bash
# Запуск desktop-приложения
./gradlew :composeApp:runDesktop
```

**Альтернатива:** Запуск из IntelliJ IDEA:
1. Откройте проект в IntelliJ IDEA
2. Убедитесь, что `local.properties` настроен с вашими учётными данными
3. Запустите desktop main функцию (Shift + F10)

**Примечание:** Все платформы используют одни и те же учётные данные из `local.properties`, которые встраиваются во время сборки через плагин BuildKonfig.

## Сборка распространяемых пакетов

### Android APK

```bash
# Debug APK
./gradlew :composeApp:assembleDebug

# Release APK (требуется настройка подписи)
./gradlew :composeApp:assembleRelease

# Результат: composeApp/build/outputs/apk/
```

### Desktop-установщики

```bash
# Пакет для текущей ОС
./gradlew :composeApp:packageDistributionForCurrentOS

# Результат:
# - macOS: composeApp/build/compose/binaries/main/dmg/
# - Windows: composeApp/build/compose/binaries/main/msi/
# - Linux: composeApp/build/compose/binaries/main/deb/
```

## Функции

- Чат в реальном времени с GigaChat AI и моделями HuggingFace
- **Сжатие диалога** (НОВОЕ в Дне 9):
  - Автоматическое сжатие разговора при достижении порога сообщений
  - Ручное сжатие через кнопку создания/редактирования (📝) в панели инструментов
  - Всегда заменяет историю резюме (режим сжатия)
  - Настраиваемый порог сообщений (4-50 сообщений)
  - Уменьшает использование токенов при сохранении контекста разговора
- **Улучшения UI/UX** (День 8):
  - Разбивка токенов с иконками-стрелками: "↑42 + ↓114" (↑=ввод, ↓=вывод)
  - Название модели в заголовке приложения
  - Автоматическая обрезка пробелов в ответах AI
- **Метаданные ответа** (День 7):
  - Отображение времени выполнения для каждого ответа AI
  - Разбивка использования токенов (запрос + ответ)
  - Мониторинг производительности и осведомлённость о стоимости
- **Копирование сообщений** (День 6):
  - Копирование отдельных сообщений одним кликом
  - Копирование всей истории разговора
  - Кроссплатформенная поддержка буфера обмена (Android, Desktop)
  - Форматированный экспорт для удобного обмена и анализа
- **Тестирование параметра температуры** (День 6):
  - Подробное руководство с 6 тестовыми вопросами
  - Демонстрация влияния температуры на ответы AI
  - Лучшие практики для разных сценариев использования
- **Сохранение истории чата при смене системного промпта** (День 5):
  - Переключатель для сохранения разговора при смене режимов
  - Системный промпт обновляется без потери контекста чата
  - Настраивается в Настройках
- **Режим структурированного XML-ответа** (День 5):
  - AI отвечает в строгом XML-формате
  - Включает резюме вопроса, ответ, роль эксперта и символы
  - Дополняет существующий JSON-режим для систем, предпочитающих XML
- **Режим пошагового рассуждения** - AI решает задачи пошагово:
  - Разбивает сложные задачи на логические шаги
  - Показывает рассуждения на каждом шаге
  - Даёт чёткие финальные ответы с объяснением
  - Идеально для математических, логических и аналитических вопросов
- **Режим экспертной панельной дискуссии** - Симуляция обсуждения экспертов:
  - 3-4 разнообразных эксперта с уникальными точками зрения
  - Реалистичные дебаты и обсуждение
  - Выводы на основе консенсуса
  - Множество точек зрения на любую тему
- **Режим диалога** - Интерактивный сбор требований через беседу:
  - AI задаёт уточняющие вопросы по одному
  - Постепенно выстраивает контекст через диалог
  - Собирает всю необходимую информацию перед итоговым результатом
  - Идеально для создания технических спецификаций, планов проектов и требований
- **Режим структурированного JSON-ответа** - AI отвечает в строгом JSON-формате с:
  - Резюме вопроса
  - Подробным ответом
  - Идентификацией роли эксперта
  - Релевантными unicode-символами
  - Переключением между JSON и форматированным видом
- Кроссплатформенная поддержка (Android, Desktop)
- Чистая MVI архитектура
- Управление историей разговора
- Обработка ошибок с функцией повтора
- Индикаторы загрузки
- Временные метки сообщений
- Функция очистки чата
- Настраиваемые параметры AI (температура, top-p, max tokens, repetition penalty)
- Поддержка нескольких AI-моделей (GigaChat, Llama, DeepSeek)

## Устранение неполадок

### Синхронизация Gradle не работает

```bash
./gradlew clean
./gradlew --refresh-dependencies
```

### Учётные данные не найдены или пусты

**Симптом:** Пустые учётные данные или ошибки сборки, связанные с отсутствующей конфигурацией

**Решение:**
- Убедитесь, что `local.properties` существует в корневой директории проекта
- Проверьте правильность имён свойств: `gigachat.clientId` и `gigachat.clientSecret`
- Убедитесь, что значения не пусты в `local.properties`
- Запустите `./gradlew clean` и пересоберите проект
- При использовании Android Studio/IntelliJ синхронизируйте Gradle-проект (File → Sync Project with Gradle Files)

### Сборка Android не работает

**Проверьте:**
- Android SDK установлен и `ANDROID_HOME` настроен
- Версия Target SDK соответствует установленному SDK
- Запустите `./gradlew :composeApp:dependencies` для проверки конфликтов

### Сборка Desktop не работает

**Проверьте:**
- Версия JDK 17 или выше: `java -version`
- `JAVA_HOME` настроен правильно
- Попробуйте запустить с `--stacktrace` для подробностей

### Сетевые ошибки

**Симптом:** Ошибки подключения при отправке сообщений

**Проверьте:**
- Интернет-соединение активно
- GigaChat API доступен
- Учётные данные API действительны
- Проверьте лимиты API

### Ошибки внедрения Koin

**Симптом:** `NoBeanDefFoundException` или подобное

**Решение:**
- Убедитесь, что `initKoin()` вызывается до создания composables
- Проверьте, что все зависимости правильно определены в модулях Koin
- Убедитесь в правильности создания платформо-специфичных модулей

## Конфигурация проекта

### Выбор режима ответа (обновлено в Дне 5)

Приложение теперь имеет шесть различных режимов ответа, доступных через выпадающее меню в Настройках:

1. Откройте Настройки (иконка шестерёнки)
2. Найдите выпадающий селектор "Response Mode"
3. Выберите один из шести вариантов:
   - **Normal Mode** (по умолчанию): Стандартные разговорные ответы AI
   - **Structured JSON Response**: AI возвращает данные в строгом JSON-формате
   - **Structured XML Response** (НОВОЕ): AI возвращает данные в строгом XML-формате
   - **Dialog Mode**: AI задаёт уточняющие вопросы для сбора полной информации
   - **Step-by-Step Reasoning**: AI решает задачи пошагово
   - **Expert Panel Discussion**: AI симулирует обсуждение экспертной панели

### Настройка сохранения истории чата (НОВОЕ в Дне 5)

Находится в Настройках, этот переключатель контролирует поведение при смене режимов ответа:

1. Откройте Настройки (иконка шестерёнки)
2. Найдите переключатель "Preserve Chat History" под селектором Response Mode
3. **ВЫКЛ** (по умолчанию): Смена режима ответа очищает чат
4. **ВКЛ**: Смена режима ответа сохраняет историю чата, обновляется только системный промпт

Используйте, когда хотите:
- Продолжить разговор с другим подходом AI
- Сохранить контекст при переключении между стилями рассуждения
- Экспериментировать с разными режимами без потери истории чата

**Подробности режимов ответа:**

#### Normal Mode
- AI напрямую отвечает на ваши вопросы
- Разговорное и естественное взаимодействие
- Лучше всего для общих вопросов и обычного общения

#### Режим структурированного JSON-ответа
- AI отвечает в строгом JSON-формате с:
  - Резюме вопроса
  - Подробным ответом
  - Ролью эксперта
  - Unicode emoji символами
- Нажмите кнопку "Format" для красиво отформатированного ответа
- Идеально для извлечения структурированных данных

#### Режим структурированного XML-ответа (НОВОЕ в Дне 5)
- AI отвечает в строгом XML-формате с:
  - Резюме вопроса
  - Подробным ответом
  - Ролью эксперта
  - Unicode emoji символами
- Аналогичен JSON-режиму, но для рабочих процессов, предпочитающих XML
- Идеально для интеграции с XML-системами

**Пример XML-ответа:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<response>
  <question_short>Как приготовить пиццу?</question_short>
  <answer>Приготовьте тесто из муки, воды, дрожжей, соли. Дайте подняться, раскатайте, добавьте начинку, выпекайте при 250°C.</answer>
  <responder_role>Шеф-повар</responder_role>
  <unicode_symbols>🍕👨‍🍳🔥</unicode_symbols>
</response>
```

#### Режим диалога (рекомендуется для сбора требований)
- AI задаёт уточняющие вопросы **по одному** (никогда несколько вопросов сразу)
- Постепенно выстраивает контекст через диалог
- Собирает всю необходимую информацию перед итоговым результатом
- Идеально для:
  - Создания технических спецификаций
  - Генерации требований проекта
  - Планирования архитектуры системы
  - Составления подробной документации
  - Сбора бизнес-требований

#### Режим пошагового рассуждения (НОВОЕ в Дне 4)
- AI разбивает задачи на пронумерованные шаги
- Показывает рассуждения на каждом этапе
- Предоставляет чёткую структуру:
  - Понимание задачи
  - Исходные данные
  - Пошаговое решение
  - Итоговый ответ/Заключение
- Идеально для:
  - Математических задач
  - Логических головоломок
  - Аналитических вопросов
  - Изучения сложных концепций

**Пример пошагового рабочего процесса:**
```
Пользователь: "Если поезд проезжает 120 км за 2 часа, сколько времени займёт путь в 300 км?"

AI:
**Понимание задачи:**
Нужно найти время, необходимое поезду для преодоления 300 км.

**Исходные данные:**
- Пройденное расстояние: 120 км
- Затраченное время: 2 часа
- Новое расстояние для расчёта: 300 км

**Шаг 1: Вычислить скорость поезда**
Скорость = 120 км / 2 часа = 60 км/ч

**Шаг 2: Вычислить время для нового расстояния**
Время = 300 км / 60 км/ч = 5 часов

**Итоговый ответ:**
Поезду потребуется **5 часов**, чтобы проехать 300 км.
```

#### Режим экспертной панельной дискуссии (НОВОЕ в Дне 4)
- AI симулирует 3-4 разнообразных экспертов, обсуждающих тему
- Каждый эксперт даёт уникальную точку зрения, основанную на своём опыте
- Включает реалистичные дебаты с согласиями и разногласиями
- Формирует консенсусное заключение, объединяющее разные мнения
- Идеально для:
  - Бизнес-решений
  - Карьерных советов
  - Выбора технической архитектуры
  - Любой темы, требующей разносторонней экспертизы

**Пример рабочего процесса экспертной панели:**
```
Пользователь: "Какой язык изучать первым - Python или JavaScript?"

AI:
**Представление панели:**
Сегодняшняя панель состоит из экспертов в области IT-образования:

**Эксперт 1 - Старший инженер-программист:** "Алексей Чен"
"Python для начинающих - чистый синтаксис, отлично для изучения концепций..."

**Эксперт 2 - Руководитель веб-разработки:** "Сара Миллер"
"JavaScript даёт мгновенную визуальную обратную связь при создании сайтов..."

**Эксперт 3 - Карьерный консультант:** "Михаил Томпсон"
"Ответ зависит от ваших целей - JS для веба, Python для data science..."

**Консенсусное заключение:**
Выбирайте Python для data science/AI, JavaScript для веб-разработки...
```

**Важно:** Режимы ответа являются взаимоисключающими - только один может быть активен в данный момент. Смена режима автоматически обновит интерфейс чата.

### Использование копирования сообщений (НОВОЕ в Дне 6)

Приложение предоставляет два способа копирования сообщений:

#### Копирование отдельных сообщений
1. Найдите любое сообщение в чате
2. Нажмите иконку копирования (📋) в правом нижнем углу сообщения
3. Содержимое сообщения теперь в вашем буфере обмена
4. Вставьте в любое место с помощью Ctrl+V (Windows/Linux) или Cmd+V (macOS)

**Сценарии использования:**
- Сохранение интересных ответов AI
- Цитирование конкретных частей разговора
- Обмен отдельными идеями

#### Копирование всего разговора
1. Нажмите иконку копирования (📋) на верхней панели (рядом с кнопками Настроек и Очистки)
2. Вся история разговора копируется в форматированный текст
3. Вставьте в любой текстовый редактор или документ

**Формат вывода:**
```
User: Первый вопрос

AI: Первый ответ

User: Второй вопрос

AI: Второй ответ
```

**Сценарии использования:**
- Архивирование полных разговоров
- Обмен полным контекстом с коллегами
- Анализ паттернов разговора
- Создание документации из взаимодействий с AI
- Экспорт для дальнейшей обработки

**Примечание:** Кнопки копирования активны только при наличии сообщений в чате. Системные сообщения исключаются при копировании всего разговора.

### Тестирование параметра температуры (НОВОЕ в Дне 6)

Для экспериментов с параметром Temperature и наблюдения его эффектов:

1. Откройте Настройки (иконка ⚙️)
2. Настройте слайдер Temperature (0.0 - 2.0)
3. Используйте тестовые вопросы из `QUESTIONS.md`:
   - **Temperature 0.0**: Детерминированные, последовательные ответы
   - **Temperature 0.7**: Баланс креативности и надёжности
   - **Temperature 1.2**: Высокая креативность и разнообразие
4. Копируйте ответы с помощью кнопок копирования для сравнения
5. Попробуйте задать один и тот же вопрос несколько раз при разных температурах

**Рекомендуемый рабочий процесс:**
1. Установите Temperature на 0.0
2. Задайте вопрос из QUESTIONS.md
3. Скопируйте ответ
4. Измените Temperature на 0.7
5. Задайте тот же вопрос снова
6. Скопируйте и сравните ответы
7. Повторите с Temperature 1.2
8. Проанализируйте различия

Смотрите `QUESTIONS.md` для 6 подобранных вопросов, демонстрирующих эффекты температуры.

### Смена AI-модели

Перейдите в Настройки (иконка ⚙️) и выберите из доступных моделей:
- GigaChat (Сбербанк)
- Llama 3.2 3B Instruct (HuggingFace)
- Meta Llama 3 70B Instruct (HuggingFace)
- DeepSeek V3 (HuggingFace)

### Настройка параметров AI

Редактируйте настройки в UI приложения:
- Temperature (0.0-2.0): Контролирует случайность
- Top P (0.0-1.0): Порог nucleus sampling
- Max Tokens (1-8192): Лимит длины ответа
- Repetition Penalty (0.0-2.0): Уменьшает повторения

## Разработка

### Добавление новых функций

1. **Data Layer**: Добавьте модели, методы API, методы репозитория
2. **Domain Layer**: Создайте use cases для бизнес-логики
3. **Presentation Layer**: Определите новые intents и обновите состояние
4. **UI Layer**: Создайте composable компоненты

### Запуск тестов

```bash
./gradlew test
```

### Стиль кода

Проект следует [Официальному стилю кода Kotlin](https://kotlinlang.org/docs/coding-conventions.html).

## Лицензия

Этот проект создан в образовательных целях.

## Контакты

По вопросам и проблемам обращайтесь к документации GigaChat API:
- [GigaChat API Docs](https://developers.sber.ru/docs/ru/gigachat/api/overview)

## Файлы документации

Проект включает подробную документацию:

- **README.md** - Полное руководство по настройке и использованию (английский)
- **README_RU.md** (этот файл) - Полное руководство по настройке и использованию (русский)
- **CLAUDE.md** - Техническая документация для интеграции с Claude Code
- **DIFF_08-09.md** - Подробный журнал изменений между Днём 8 и Днём 9
- **COMPRESSION_TEST_SCENARIOS.md** - Тестовые сценарии для функции сжатия диалога
- **QUESTIONS.md** - Руководство по тестированию параметра температуры с 6 подобранными вопросами

## Благодарности

- Создано с [Kotlin Multiplatform](https://kotlinlang.org/docs/multiplatform.html)
- UI на базе [Compose Multiplatform](https://www.jetbrains.com/lp/compose-multiplatform/)
- AI от [GigaChat](https://developers.sber.ru/portal/products/gigachat)

## Видео

- https://disk.yandex.ru/i/4OSjrB53mcvoZw

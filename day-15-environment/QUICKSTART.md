# Quick Start Guide: Docker + Android Emulator Pipeline

Быстрый старт для локального тестирования на вашей машине перед настройкой VPS.

## 🚀 Локальное тестирование (MacOS/Linux)

### Шаг 1: Установка зависимостей

```bash
# Установка Python зависимостей
cd mcp-servers
python3 -m venv venv
source venv/bin/activate
pip install sse-starlette starlette uvicorn httpx python-dotenv docker
```

### Шаг 2: Запуск Docker MCP Server

```bash
# Запуск Docker сервера (работает на Mac/Linux)
cd mcp-servers
source venv/bin/activate
python launcher.py docker --no-auth

# Проверка работы
curl http://localhost:8006/health
```

### Шаг 3: Тестирование Docker операций

Откройте AI Agent приложение и добавьте сервер:
- **Name:** Local Docker
- **URL:** http://localhost:8006
- **Transport:** SSE
- **Enabled:** ✓

Попробуйте команды:
```
Покажи список Docker контейнеров
```

```
Покажи все Docker образы
```

```
Запусти контейнер nginx на порту 8080
```

---

## 📱 Android Emulator (требует Android SDK)

### Шаг 1: Установка Android SDK

**MacOS (Homebrew):**
```bash
brew install --cask android-commandlinetools
```

**Linux (Manual):**
```bash
# См. полную инструкцию в VPS_SETUP_GUIDE.md
```

### Шаг 2: Создание AVD

```bash
# Установка компонентов SDK
sdkmanager "platform-tools" \
           "platforms;android-34" \
           "build-tools;34.0.0" \
           "emulator" \
           "system-images;android-34;google_apis;x86_64"

# Создание эмулятора
avdmanager create avd \
  -n pixel6_api34 \
  -k "system-images;android-34;google_apis;x86_64" \
  -d "pixel_6"
```

### Шаг 3: Запуск ADB MCP Server

```bash
cd mcp-servers
source venv/bin/activate
python launcher.py adb --no-auth

# Проверка работы
curl http://localhost:8007/health
```

### Шаг 4: Тестирование ADB операций

Добавьте сервер в приложение:
- **Name:** Local ADB
- **URL:** http://localhost:8007
- **Transport:** SSE
- **Enabled:** ✓

Попробуйте команды:
```
Покажи доступные AVD
```

```
Запусти эмулятор pixel6_api34
```

```
Сделай скриншот экрана
```

```
Покажи информацию об устройстве
```

---

## 🐳 Docker Build для Android

### Шаг 1: Сборка Docker образа

```bash
cd mcp-servers/docker
docker compose build android-builder

# Проверка образа
docker images | grep android-builder
```

### Шаг 2: Тестовая сборка проекта

```bash
# Копирование вашего Android проекта в workspace
cp -r /path/to/your/android/project mcp-servers/docker/workspace/myproject

# Запуск контейнера
docker compose up -d android-builder

# Выполнение сборки внутри контейнера
docker exec android_builder bash -c "cd myproject && ./gradlew assembleDebug"

# Проверка APK
docker exec android_builder ls -lh myproject/app/build/outputs/apk/debug/
```

### Шаг 3: Автоматическая сборка через Docker MCP

Используйте Docker MCP Server для выполнения команд:

```
Выполни команду в контейнере android_builder: cd workspace/myproject && ./gradlew assembleDebug
```

```
Покажи логи контейнера android_builder
```

---

## ✅ Полный цикл: Build + Install + Screenshot

### Пример работы через AI Agent:

**Шаг 1:** Сборка APK
```
Собери APK в контейнере android_builder: cd workspace/myproject && ./gradlew assembleDebug
```

**Шаг 2:** Копирование APK из контейнера
```
Выполни команду: docker cp android_builder:/home/android/workspace/myproject/app/build/outputs/apk/debug/app-debug.apk ./app-debug.apk
```

**Шаг 3:** Запуск эмулятора
```
Запусти эмулятор pixel6_api34
```

**Шаг 4:** Установка APK
```
Установи APK из ./app-debug.apk на эмулятор
```

**Шаг 5:** Скриншот
```
Сделай скриншот экрана
```

---

## 🎥 Демонстрация для курса

### Что записать на видео:

1. **Запуск серверов** (10 сек)
   ```bash
   python launcher.py docker adb --no-auth
   ```

2. **Добавление серверов в приложении** (20 сек)
   - Показать экран настроек
   - Добавить Docker и ADB серверы

3. **Голосовая команда: "Покажи Docker контейнеры"** (10 сек)
   - AI показывает список контейнеров

4. **Голосовая команда: "Запусти Android эмулятор"** (30 сек)
   - AI запускает эмулятор
   - Показать что эмулятор действительно запустился

5. **Голосовая команда: "Сделай скриншот"** (15 сек)
   - AI делает скриншот
   - Показать полученный скриншот в чате

6. **Голосовая команда: "Собери APK проекта"** (40 сек)
   - AI запускает сборку в Docker
   - Показать логи сборки

7. **Голосовая команда: "Установи собранный APK"** (20 сек)
   - AI устанавливает APK на эмулятор
   - Показать установленное приложение

**Итого:** ~2.5 минуты видео

---

## 🌐 Переход на VPS

После успешного локального тестирования:

1. Следуйте полной инструкции в `VPS_SETUP_GUIDE.md`
2. Настройте VPS сервер с Ubuntu 24.04
3. Запустите те же MCP серверы на VPS
4. Подключитесь из мобильного приложения к VPS по IP

---

## 🐛 Troubleshooting

### Docker MCP Server не запускается
```bash
# Проверка Docker
docker version

# Проверка прав
sudo usermod -aG docker $USER
newgrp docker
```

### ADB не находится
```bash
# Проверка ADB
which adb
adb version

# Установка Android SDK
# MacOS: brew install --cask android-commandlinetools
# Linux: см. VPS_SETUP_GUIDE.md
```

### Эмулятор не запускается
```bash
# Проверка KVM (только Linux)
kvm-ok

# Список AVD
avdmanager list avd

# Ручной запуск для отладки
emulator -avd pixel6_api34 -no-window -no-audio
```

### MCP Server недоступен
```bash
# Проверка портов
lsof -i :8006
lsof -i :8007

# Логи
python launcher.py docker adb --no-auth
```

---

## 📚 Дополнительные материалы

- **VPS_SETUP_GUIDE.md** - Полная инструкция по настройке VPS
- **COMMANDS_CHEATSHEET.md** - Шпаргалка по командам
- **DOCKER_MCP_GUIDE.md** - Руководство по Docker MCP Server

---

## 🎯 Следующие шаги

1. ✅ Протестировать локально
2. ✅ Записать видео демонстрацию
3. ⏳ Настроить VPS сервер
4. ⏳ Настроить HTTPS и безопасность
5. ⏳ Добавить CI/CD интеграцию

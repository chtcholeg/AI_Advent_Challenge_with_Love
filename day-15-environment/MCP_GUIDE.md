# Руководство по интеграции MCP-серверов

## Обзор

GigaChat Мультиплатформенное Чат-Приложение теперь поддерживает **серверы Model Context Protocol (MCP)**, позволяя AI подключаться к внешним инструментам и источникам данных. Это открывает такие возможности как:

- Чтение и запись файлов
- Запросы к базам данных
- Поиск в интернете
- Выполнение системных команд
- Доступ к API
- И многое другое!

## Как это работает

1. **Настройка MCP-серверов**: Добавьте локальные (stdio) или удалённые (HTTP) MCP-серверы через экран Настроек
2. **Обнаружение инструментов AI**: При включении AI автоматически обнаруживает доступные инструменты от подключённых серверов
3. **Вызов функций**: AI может вызывать эти инструменты во время разговоров для получения информации или выполнения действий
4. **Бесшовная интеграция**: Выполнение инструментов происходит прозрачно - AI автоматически решает, когда использовать инструменты

## Добавление MCP-серверов

### Через экран Настроек

1. Откройте **Настройки** из экрана чата
2. Нажмите на карточку **MCP Servers**
3. Нажмите кнопку **+** (Добавить)
4. Заполните данные сервера:
   - **Имя сервера**: Понятное имя для идентификации
   - **Тип сервера**: Выберите между Local (stdio) или Remote (HTTP)
   - **Конфигурация**: Укажите детали в зависимости от типа (см. ниже)
5. Нажмите **Add** для сохранения

### Типы серверов

#### Локальные (stdio) серверы

Локальные серверы запускаются как отдельные процессы на вашем устройстве и взаимодействуют через стандартный ввод/вывод.

**Поля конфигурации**:
- **Command**: Исполняемый файл для запуска (например, `node`, `python`, `java`)
- **Arguments**: Аргументы команды, разделённые пробелами

**Пример**: Node.js MCP-сервер
```
Command: node
Arguments: /path/to/mcp-server/index.js
```

**Пример**: Python MCP-сервер
```
Command: python
Arguments: -m mcp_server.main --port 3000
```

#### Удалённые (HTTP) серверы

Удалённые серверы доступны по HTTP/HTTPS с опциональной аутентификацией.

**Поля конфигурации**:
- **Server URL**: Полный URL включая протокол (например, `https://mcp.example.com`)
- **Auth Token**: Опциональный Bearer-токен для аутентификации

**Пример**: Размещённый MCP-сервер
```
Server URL: https://mcp-tools.mycompany.com
Auth Token: sk-abc123xyz789
```

## Популярные примеры MCP-серверов

### 1. Файловый сервер (stdio)

Позволяет AI читать и записывать файлы на вашем устройстве.

**Установка**:
```bash
npm install -g @modelcontextprotocol/server-filesystem
```

**Конфигурация**:
```
Имя сервера: Filesystem
Тип сервера: Local (stdio)
Command: mcp-server-filesystem
Arguments: /path/to/allowed/directory
```

**Предоставляемые инструменты**:
- `read_file`: Чтение содержимого файла
- `write_file`: Запись или обновление файлов
- `list_directory`: Список файлов в директории
- `create_directory`: Создание новых директорий
- `delete_file`: Удаление файлов

### 2. SQLite Database сервер (stdio)

Позволяет AI выполнять запросы к SQLite базам данных.

**Установка**:
```bash
npm install -g @modelcontextprotocol/server-sqlite
```

**Конфигурация**:
```
Имя сервера: SQLite
Тип сервера: Local (stdio)
Command: mcp-server-sqlite
Arguments: /path/to/database.db
```

**Предоставляемые инструменты**:
- `query`: Выполнение SQL SELECT запросов
- `execute`: Запуск SQL INSERT/UPDATE/DELETE команд
- `list_tables`: Показать все таблицы
- `describe_table`: Получить схему таблицы

### 3. Web Search сервер (HTTP)

Предоставляет возможности веб-поиска через удалённый API.

**Конфигурация**:
```
Имя сервера: Web Search
Тип сервера: Remote (HTTP)
Server URL: https://api.websearch-mcp.com
Auth Token: ваш_api_ключ
```

**Предоставляемые инструменты**:
- `search_web`: Поиск в интернете
- `get_page_content`: Получение содержимого веб-страницы
- `summarize_results`: Суммаризация результатов поиска

### 4. Weather сервер (stdio)

Пример простого кастомного MCP-сервера.

**Установка**: Создайте Node.js скрипт `weather-server.js`

**Конфигурация**:
```
Имя сервера: Weather
Тип сервера: Local (stdio)
Command: node
Arguments: /path/to/weather-server.js
```

**Предоставляемые инструменты**:
- `get_weather`: Получение текущей погоды для местоположения
- `get_forecast`: Получение прогноза на 5 дней

### 5. Calculator сервер (stdio)

Инструменты для математических вычислений.

**Конфигурация**:
```
Имя сервера: Calculator
Тип сервера: Local (stdio)
Command: python
Arguments: -m mcp_calculator
```

**Предоставляемые инструменты**:
- `calculate`: Вычисление математических выражений
- `convert_units`: Конвертация единиц измерения
- `statistics`: Расчёт среднего, медианы, стандартного отклонения

## Управление серверами

### Включение/Выключение серверов

Используйте переключатель на каждой карточке сервера для включения или отключения. Отключённые серверы не будут подключены, и их инструменты не будут доступны AI.

### Подключение/Отключение

- **Connect**: Вручную установить подключение к серверу
- **Disconnect**: Закрыть подключение, сохранив конфигурацию сервера

### Редактирование сервера

Нажмите кнопку **Edit** для изменения конфигурации сервера. Обратите внимание, что после внесения изменений потребуется переподключение.

### Удаление сервера

Нажмите кнопку **Delete** и подтвердите для полного удаления конфигурации сервера.

## Статус подключения

Каждый сервер отображает текущий статус:

- 🟢 **Connected**: Сервер активен и инструменты доступны
- 🟡 **Connecting**: Подключение в процессе
- 🔴 **Error**: Подключение не удалось (проверьте конфигурацию и логи)
- ⚪ **Disconnected**: Сервер не подключён

## Использование MCP-инструментов в разговорах

После подключения серверов AI автоматически знает о доступных инструментах и будет использовать их при необходимости:

**Пример разговора**:
```
Пользователь: Какие файлы в моей папке Documents?

AI: [Вызывает инструмент filesystem: list_directory(/Users/user/Documents)]
    Вот файлы в вашей папке Documents:
    - report.pdf
    - notes.txt
    - project/
    ...
```

**Пример с базой данных**:
```
Пользователь: Сколько пользователей в базе данных?

AI: [Вызывает инструмент SQLite: query("SELECT COUNT(*) FROM users")]
    В базе данных 1,247 пользователей.
```

AI автоматически решает, когда использовать инструменты, основываясь на ваших вопросах и доступных возможностях.

## Устранение неполадок

### Сервер не подключается

**Проблема**: Статус показывает "Error" или "Connecting" бесконечно

**Решения**:
1. **Проверьте команду/путь**: Убедитесь, что команда существует и находится в PATH
2. **Проверьте аргументы**: Проверьте опечатки в путях к файлам или аргументах
3. **Протестируйте вручную**: Попробуйте запустить команду в терминале
4. **Проверьте разрешения**: Убедитесь, что приложение имеет разрешение на выполнение команд (Android может ограничивать это)
5. **Просмотрите логи**: Проверьте логи приложения для подробных сообщений об ошибках

### Инструменты недоступны

**Проблема**: Сервер подключён, но AI не использует инструменты

**Решения**:
1. **Подтвердите подключение**: Проверьте, что статус "Connected" (зелёный)
2. **Проверьте поддержку модели**: Только модели GigaChat в настоящее время поддерживают вызов функций
3. **Проверьте совместимость сервера**: Убедитесь, что MCP-сервер реализует правильную версию протокола
4. **Перезапустите подключение**: Отключите и переподключите сервер

### Проблемы специфичные для Android

**Проблема**: stdio-серверы не работают на Android

**Примечание**: Android имеет ограничения на выполнение произвольных команд. stdio-серверы могут не работать на всех устройствах Android.

**Альтернативы**:
1. Используйте HTTP-серверы вместо них (полностью поддерживаются на Android)
2. Рассмотрите создание компаньон-приложения, которое выставляет MCP через HTTP
3. Используйте облачные MCP-серверы

### Проблемы специфичные для Desktop

**Проблема**: Команда не найдена

**Решения**:
1. Используйте абсолютные пути: `/usr/local/bin/node` вместо `node`
2. Проверьте переменную окружения PATH
3. Убедитесь, что команда установлена: `which node` или `where node`

## Лучшие практики

### Безопасность

1. **Ограничьте доступ к файловой системе**: Предоставляйте доступ только к необходимым директориям
2. **Используйте read-only где возможно**: Предпочитайте инструменты только для чтения, когда запись не нужна
3. **Проверяйте удалённые серверы**: Подключайтесь только к доверенным HTTP эндпоинтам
4. **Защищайте токены**: Храните токены аутентификации в безопасности и не делитесь ими
5. **Просматривайте описания инструментов**: Поймите, что может делать каждый инструмент, перед включением

### Производительность

1. **Включайте только нужные серверы**: Отключайте серверы, которые не используете активно
2. **Используйте специфичные инструменты**: Настраивайте серверы с минимальным, целенаправленным набором инструментов
3. **Следите за использованием токенов**: Вызовы инструментов увеличивают потребление токенов
4. **Кэшируйте результаты**: Рассмотрите серверы, которые кэшируют часто запрашиваемые данные

### Организация

1. **Используйте описательные имена**: Называйте серверы понятно (например, "Project Docs" вместо "FS1")
2. **Группируйте связанные инструменты**: Настраивайте серверы по назначению (Files, Database, API и т.д.)
3. **Документируйте конфигурации**: Ведите заметки о настройках кастомных серверов
4. **Тестируйте постепенно**: Добавляйте и тестируйте серверы по одному

## Создание кастомных MCP-серверов

Хотите создать свой MCP-сервер? Вот минимальный пример на Node.js:

```javascript
// simple-mcp-server.js
const { Server } = require('@modelcontextprotocol/sdk');

const server = new Server({
  name: 'My Custom Server',
  version: '1.0.0'
});

// Определение инструмента
server.addTool({
  name: 'hello',
  description: 'Приветствует кого-то',
  inputSchema: {
    type: 'object',
    properties: {
      name: { type: 'string', description: 'Имя для приветствия' }
    },
    required: ['name']
  },
  handler: async ({ name }) => {
    return { content: `Привет, ${name}!` };
  }
});

// Запуск stdio транспорта
server.connect('stdio');
```

**Ресурсы**:
- [Спецификация MCP протокола](https://modelcontextprotocol.io)
- [Официальный MCP SDK](https://github.com/modelcontextprotocol/kotlin-sdk)
- [Примеры серверов](https://github.com/modelcontextprotocol/servers)

## FAQ

**В: Какие AI-модели поддерживают MCP-инструменты?**
О: В настоящее время только модели GigaChat (GigaChat, GigaChat-Plus, GigaChat-Pro) поддерживают вызов функций. Модели HuggingFace пока не поддерживают эту функцию.

**В: Можно ли использовать несколько серверов одновременно?**
О: Да! Включите столько серверов, сколько нужно. AI будет иметь доступ к инструментам со всех подключённых серверов.

**В: Есть ли затраты токенов на использование инструментов?**
О: Да. Описания инструментов отправляются с каждым запросом, и результаты выполнения инструментов учитываются в использовании токенов. Приложение отображает общее количество токенов после каждого ответа.

**В: Могут ли инструменты получить доступ к интернету?**
О: Только если сам MCP-сервер имеет доступ к интернету. Приложение не ограничивает, что могут делать серверы - безопасность зависит от реализации сервера.

**В: Как обновить конфигурацию сервера?**
О: Нажмите кнопку Edit, внесите изменения и сохраните. Вам нужно будет отключить и переподключить сервер для применения изменений.

**В: Можно ли экспортировать/импортировать конфигурации серверов?**
О: Пока нет, но эта функция запланирована для будущего обновления.

**В: Что происходит при ошибке вызова инструмента?**
О: Ошибка возвращается AI, который обычно объяснит вам проблему и может предложить альтернативы.

## Поддержка и обратная связь

Если вы столкнулись с проблемами или имеете предложения по интеграции MCP:

1. Сначала проверьте это руководство и раздел устранения неполадок
2. Просмотрите логи приложения для подробных сообщений об ошибках
3. Протестируйте подключение к серверу вне приложения
4. Сообщайте об ошибках через трекер проекта

---

**Версия**: 1.0
**Последнее обновление**: День 11 AI Advent Challenge
**Совместимость**: GigaChat Multiplatform Chat Application v1.0+

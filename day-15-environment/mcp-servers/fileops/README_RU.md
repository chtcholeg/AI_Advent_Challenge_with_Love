# FileOps MCP Server - Русская документация

MCP сервер для работы с файлами - позволяет AI ассистенту читать, писать, искать и управлять файлами на вашем компьютере.

## 🎯 Что это дает?

AI сможет:
- ✅ Сохранять разговоры и данные в файлы
- ✅ Читать конфигурационные файлы
- ✅ Искать файлы по имени и содержимому
- ✅ Анализировать код в проектах
- ✅ Создавать отчеты и сохранять их
- ✅ Управлять файловой структурой

## 🚀 Быстрый старт

### 1. Запуск сервера

```bash
# Через launcher
python launcher.py fileops --no-auth

# Или напрямую
python -m fileops.main --no-auth
```

### 2. Проверка работы

```bash
curl http://localhost:8005/health
```

### 3. Добавьте в приложение

В настройках MCP серверов добавьте:
- Имя: FileOps
- Хост: localhost
- Порт: 8005
- Транспорт: HTTP

## 🛠️ Инструменты (8 штук)

### 1. write_file - Запись в файл
Создает или обновляет файл.

**Параметры:**
- `path` - путь к файлу
- `content` - содержимое
- `append` - добавить в конец (необязательно)

**Пример:**
```
Вы: "Сохрани наш разговор в файл chat.txt"
AI: Использует write_file для сохранения
```

### 2. read_file - Чтение файла
Читает содержимое файла.

**Параметры:**
- `path` - путь к файлу

**Пример:**
```
Вы: "Прочитай config.json"
AI: Использует read_file и покажет содержимое
```

### 3. list_directory - Список файлов
Показывает файлы в директории.

**Параметры:**
- `path` - путь к директории (необязательно)
- `pattern` - фильтр (например, *.py)

**Пример:**
```
Вы: "Покажи все Python файлы в проекте"
AI: list_directory с pattern="*.py"
```

### 4. search_files - Поиск по имени
Ищет файлы по glob-паттерну.

**Параметры:**
- `pattern` - паттерн (например, test_*.py)
- `path` - где искать
- `recursive` - рекурсивно (по умолчанию true)

**Пример:**
```
Вы: "Найди все тестовые файлы"
AI: search_files с pattern="test_*.py"
```

### 5. search_content - Поиск в содержимом
Ищет текст внутри файлов.

**Параметры:**
- `query` - что искать
- `file_pattern` - в каких файлах (например, *.py)
- `path` - где искать
- `case_sensitive` - учитывать регистр
- `regex` - использовать регулярные выражения

**Пример:**
```
Вы: "Найди все TODO комментарии в коде"
AI: search_content query="TODO" file_pattern="*.py"
```

### 6. delete_file - Удаление файла
Удаляет файл.

**Параметры:**
- `path` - путь к файлу

### 7. create_directory - Создание директории
Создает директорию (и родительские если нужно).

**Параметры:**
- `path` - путь к директории

### 8. get_file_info - Информация о файле
Показывает метаданные файла.

**Параметры:**
- `path` - путь к файлу/директории

## 🔒 Безопасность

### Root Directory Isolation
Все операции ограничены корневой директорией. Невозможно получить доступ к файлам вне этой директории.

**Настройка безопасной директории:**
```bash
python -m fileops.main --no-auth --root-dir ~/Documents/ai_workspace
```

### Ограничения размера
По умолчанию:
- Максимальный размер файла: 10MB
- Максимум результатов поиска: 100

**Изменение лимитов:**
```bash
export FILEOPS_MAX_FILE_SIZE=52428800  # 50MB
export FILEOPS_MAX_SEARCH_RESULTS=200
python launcher.py fileops --no-auth
```

### Аутентификация
Для защищенного доступа используйте API ключ:

```bash
export MCP_API_KEY=ваш_секретный_ключ
python launcher.py fileops
```

И укажите ключ в настройках приложения.

## 📝 Примеры использования

### Сохранение разговора
```
Вы: "Сохрани наш разговор в файл conversations/today.txt"
AI: [Использует write_file]
     "Сохранил разговор в conversations/today.txt (1234 байт)"
```

### Поиск кода
```
Вы: "Найди все места где используется async/await"
AI: [Использует search_content с query="async" и "await"]
     "Найдено в 5 файлах:
     - main.py: строки 15, 42, 89
     - client.py: строки 23, 67
     ..."
```

### Анализ проекта
```
Вы: "Покажи структуру проекта и найди TODO комментарии"
AI: [Использует list_directory и search_content]
     "Структура:
     📁 src/
       📄 main.py
       📄 config.py
     📁 tests/
       📄 test_main.py
     
     TODO комментарии (3):
     - src/main.py:45: TODO: Add error handling
     ..."
```

### Создание отчета
```
Вы: "Создай отчет о файлах в проекте и сохрани в report.txt"
AI: [Использует list_directory + search_files + write_file]
     "Создал отчет:
     - Всего файлов: 25
     - Python файлов: 12
     - Тестовых файлов: 5
     
     Сохранено в report.txt"
```

## ⚙️ Конфигурация

### Переменные окружения

```bash
# Аутентификация
MCP_API_KEY=your_secret_key

# Сервер
HOST=0.0.0.0
PORT=8005

# FileOps настройки
FILEOPS_ROOT_DIR=/path/to/workspace
FILEOPS_MAX_FILE_SIZE=10485760      # 10MB
FILEOPS_MAX_SEARCH_RESULTS=100
```

### Аргументы командной строки

```bash
python -m fileops.main [опции]

Опции:
  --no-auth           Отключить аутентификацию
  --host HOST         Адрес (по умолчанию: 0.0.0.0)
  --port PORT         Порт (по умолчанию: 8005)
  --root-dir PATH     Корневая директория
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Активируйте venv
source venv/bin/activate

# Запустите тесты
python test_fileops_simple.py
```

### Ручная проверка

```bash
# Проверка здоровья
curl http://localhost:8005/health

# Список инструментов
curl http://localhost:8005/tools

# OpenAPI документация
open http://localhost:8005/docs
```

## 🔧 Устранение неполадок

### Порт занят
```bash
# Проверьте что использует порт
lsof -i :8005

# Используйте другой порт
python -m fileops.main --no-auth --port 8006
```

### Ошибка доступа к файлу
```bash
# Проверьте права
ls -la /path/to/file

# Используйте другую директорию
python -m fileops.main --no-auth --root-dir ~/Documents
```

### Файл слишком большой
```bash
# Увеличьте лимит
export FILEOPS_MAX_FILE_SIZE=52428800  # 50MB
python launcher.py fileops --no-auth
```

## 📚 Дополнительные материалы

- **QUICKSTART.md** - Быстрое начало (English)
- **EXAMPLES.md** - Подробные примеры (English)
- **../FILEOPS_IMPLEMENTATION.md** - Технические детали
- **../START_FILEOPS.md** - Инструкция по запуску

## 🎓 Продвинутое использование

### Поиск с регулярными выражениями

```
Вы: "Найди все функции в Python файлах"
AI: search_content query="def\\s+\\w+" regex=true file_pattern="*.py"
```

### Множественные операции

```
Вы: "Создай директорию reports, сохрани туда отчет и покажи содержимое"
AI: [Использует create_directory + write_file + list_directory]
```

### Фильтрация файлов

```
Вы: "Покажи только Python файлы начинающиеся с test_"
AI: list_directory pattern="test_*.py"
```

## 💡 Советы

1. **Используйте root-dir** для ограничения доступа к конкретной директории
2. **Включайте аутентификацию** в production
3. **Настройте лимиты** под свои нужды
4. **Создайте отдельную workspace директорию** для AI

## 🤝 Интеграция

После запуска сервера, AI в вашем приложении получит доступ ко всем 8 инструментам автоматически через MCP протокол.

Никакой дополнительной настройки в коде не требуется - просто добавьте сервер в настройки!

---

**FileOps MCP Server v1.0.0** 🎊

Полностью протестирован и готов к использованию!

# 🚀 Как запустить FileOps MCP Server

## Быстрый старт

### 1. Убедитесь что вы в правильной директории
```bash
cd /Users/shchepilov/AndroidStudioProjects/AI_Advent_Challenge_with_Love/day-14-mcp-composition/mcp-servers
```

### 2. Активируйте виртуальное окружение
```bash
source venv/bin/activate
```

### 3. Запустите сервер
```bash
# Через launcher (рекомендуется)
python launcher.py fileops --no-auth

# Или напрямую
python -m fileops.main --no-auth
```

### 4. Проверьте работу
Откройте новый терминал и выполните:
```bash
curl http://localhost:8005/health
```

Должен вернуться JSON с информацией о сервере:
```json
{
  "status": "ok",
  "tools_count": 8,
  "active_sessions": 0,
  "auth_enabled": false,
  "root_directory": "/current/directory",
  "max_file_size": 10485760
}
```

## Интеграция с GigaChat приложением

1. Запустите сервер (шаги выше)
2. В приложении откройте настройки MCP серверов
3. Добавьте новый сервер:
   - **Имя**: FileOps
   - **Хост**: localhost
   - **Порт**: 8005
   - **Транспорт**: HTTP
   - **API Key**: (оставьте пустым)
   - **Включить**: ✓

4. Сохраните и проверьте подключение

## Что можно делать с FileOps?

После подключения AI сможет:

### 💾 Сохранять файлы
**Вы:** "Сохрани наш разговор в файл chat.txt"
**AI:** Использует `write_file` для сохранения

### 📖 Читать файлы
**Вы:** "Прочитай содержимое config.json"
**AI:** Использует `read_file`

### 🔍 Искать файлы
**Вы:** "Найди все Python файлы с TODO комментариями"
**AI:** Использует `search_content` с паттерном "TODO" в файлах *.py

### 📁 Управлять директориями
**Вы:** "Создай папку reports и покажи что в ней"
**AI:** Использует `create_directory` и `list_directory`

### 🗑️ Удалять файлы
**Вы:** "Удали файл temp.txt"
**AI:** Использует `delete_file`

## Конфигурация

### Изменить рабочую директорию
По умолчанию сервер работает с текущей директорией. Чтобы ограничить доступ:

```bash
python -m fileops.main --no-auth --root-dir /path/to/safe/directory
```

### Включить аутентификацию
Для production использования:

```bash
export MCP_API_KEY=your_secret_key
python launcher.py fileops
```

И добавьте API key в настройках приложения.

### Увеличить лимит размера файла
```bash
export FILEOPS_MAX_FILE_SIZE=52428800  # 50MB
python launcher.py fileops --no-auth
```

## Остановка сервера

В терминале где запущен сервер нажмите:
```
Ctrl+C
```

## Тестирование

Запустите тесты чтобы убедиться что всё работает:

```bash
# Активируйте venv если еще не активировано
source venv/bin/activate

# Запустите тесты
python test_fileops_simple.py
```

Должно вывести:
```
✓ All tests completed!
```

## Документация

- **fileops/QUICKSTART.md** - Быстрое начало
- **fileops/README.md** - Полное руководство
- **fileops/EXAMPLES.md** - Примеры использования
- **FILEOPS_IMPLEMENTATION.md** - Технические детали

## Устранение проблем

### Порт 8005 занят
```bash
# Используйте другой порт
python -m fileops.main --no-auth --port 8006
```

### Нет прав доступа к директории
```bash
# Используйте другую директорию
python -m fileops.main --no-auth --root-dir ~/Documents
```

### Сервер не отвечает
Проверьте что сервер запущен:
```bash
ps aux | grep fileops
```

Перезапустите если нужно.

---

**FileOps MCP Server v1.0.0** 🎊

Готов к использованию!

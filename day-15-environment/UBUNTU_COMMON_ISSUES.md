# Частые проблемы при настройке VPS на Ubuntu 24.04

Этот файл содержит решения для **12 частых проблем**, с которыми сталкиваются пользователи при настройке VPS на Ubuntu 24.04.

---

## 🔴 Проблема 1: "E: Invalid operation install"

### Симптомы
```
sudo apt install -y openjdk-17-jdk openjdk-17-jre
E: Invalid operation install
```

### Причина
Некоторые версии apt не могут корректно обработать установку нескольких пакетов с флагом `-y` одновременно.

### ✅ Решение
```bash
# Устанавливайте пакеты по отдельности
sudo apt update
sudo apt install openjdk-17-jdk
sudo apt install openjdk-17-jre

# Или используйте apt-get
sudo apt-get install -y openjdk-17-jdk openjdk-17-jre
```

---

## 🔴 Проблема 2: "E: Unable to locate package" (libvirt, qemu и др.)

### Симптомы
```
sudo apt install -y libvirt-daemon-system
E: Unable to locate package libvirt-daemon-system
```

или

```
E: Package 'qemu-kvm' has no installation candidate
```

### Причина
На минимальных установках Ubuntu Server репозитории **universe** и **multiverse** могут быть отключены по умолчанию. Многие пакеты (libvirt, qemu, дополнительные инструменты) находятся именно там.

### ✅ Решение
```bash
# Включаем universe и multiverse репозитории
sudo add-apt-repository universe
sudo add-apt-repository multiverse

# Обновляем список пакетов
sudo apt update

# Теперь пакеты должны быть доступны
sudo apt install -y qemu-kvm libvirt-daemon-system libvirt-clients

# Проверяем что репозитории включены
apt-cache policy | grep universe
# Должно быть несколько строк с universe
```

**Рекомендация:** Включайте universe и multiverse репозитории сразу после первого входа на VPS.

---

## 🔴 Проблема 3: "No such file or directory" при установке Docker

### Симптомы
```
tee: /etc/apt/sources.list.d/docker.list: No such file or directory
```

### Причина
На минимальных установках Ubuntu директория `/etc/apt/sources.list.d/` может отсутствовать.

### ✅ Решение
```bash
# Создайте директории вручную
sudo mkdir -p /etc/apt/keyrings
sudo mkdir -p /etc/apt/sources.list.d
sudo chmod 755 /etc/apt/keyrings
sudo chmod 755 /etc/apt/sources.list.d

# Затем повторите команды добавления репозитория Docker
```

См. подробное решение в файле **DOCKER_FIX.md**

---

## 🔴 Проблема 4: KVM не доступен

### Симптомы
```
kvm-ok
INFO: Your CPU does not support KVM extensions
KVM acceleration can NOT be used
```

### Причина
VPS не поддерживает nested virtualization или KVM модули не загружены.

### ✅ Решение
```bash
# Проверьте поддержку виртуализации
egrep -c '(vmx|svm)' /proc/cpuinfo
# Если вывод = 0, ваш VPS не поддерживает KVM

# Если вывод > 0, загрузите модули
sudo modprobe kvm
sudo modprobe kvm_intel  # для Intel
# sudo modprobe kvm_amd  # для AMD

# Проверьте снова
sudo kvm-ok
```

**Важно:** Если ваш VPS провайдер не поддерживает KVM, эмулятор будет работать очень медленно. Рассмотрите смену провайдера на Hetzner, DigitalOcean или Vultr.

---

## 🔴 Проблема 5: Python venv ошибка на Ubuntu 24.04

### Симптомы
```
error: externally-managed-environment
```

### Причина
Ubuntu 24.04 следует PEP 668 и не позволяет устанавливать пакеты глобально через pip.

### ✅ Решение
```bash
# ВСЕГДА используйте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Теперь можно устанавливать пакеты
pip install package_name

# Для выхода из venv
deactivate
```

**Никогда** не используйте `--break-system-packages` на production серверах!

---

## 🔴 Проблема 6: Docker permission denied

### Симптомы
```
docker ps
permission denied while trying to connect to the Docker daemon socket
```

### Причина
Текущий пользователь не в группе `docker`.

### ✅ Решение
```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# ВАЖНО: Перелогиньтесь или используйте
newgrp docker

# Проверьте
docker ps
```

---

## 🔴 Проблема 7: Android SDK лицензии не приняты

### Симптомы
```
Error: Failed to install the following Android SDK packages as some licences have not been accepted.
```

### ✅ Решение
```bash
# Примите все лицензии
yes | sdkmanager --licenses

# Или интерактивно
sdkmanager --licenses
# Нажмите 'y' для каждой лицензии
```

---

## 🔴 Проблема 8: Port already in use

### Симптомы
```
Error: bind: address already in use
```

### Причина
Порт уже занят другим процессом.

### ✅ Решение
```bash
# Найдите процесс на порту (например, 8006)
sudo ss -tulpn | grep 8006
# или
sudo lsof -i :8006

# Убейте процесс (замените PID)
sudo kill -9 PID

# Или найдите и убейте автоматически
sudo kill -9 $(sudo lsof -t -i:8006)
```

---

## 🔴 Проблема 9: systemd сервис не запускается

### Симптомы
```
systemctl start mcp-servers.service
Job for mcp-servers.service failed
```

### ✅ Решение
```bash
# Проверьте детальный статус
sudo systemctl status mcp-servers.service

# Проверьте логи
sudo journalctl -u mcp-servers.service -n 50

# Проверьте синтаксис service файла
sudo systemd-analyze verify /etc/systemd/system/mcp-servers.service

# Перезагрузите конфигурацию после правок
sudo systemctl daemon-reload
sudo systemctl restart mcp-servers.service
```

---

## 🔴 Проблема 10: Out of disk space

### Симптомы
```
No space left on device
```

### ✅ Решение
```bash
# Проверьте использование диска
df -h

# Найдите большие файлы
sudo du -sh /* | sort -rh | head -10

# Очистите Docker (ОСТОРОЖНО: удалит неиспользуемые образы/контейнеры)
docker system prune -a

# Очистите APT кэш
sudo apt clean
sudo apt autoclean

# Очистите journald логи (оставить 3 дня)
sudo journalctl --vacuum-time=3d

# Очистите старые ядра
sudo apt autoremove
```

---

## 🔴 Проблема 11: Git аутентификация не работает

### Симптомы
```
remote: Invalid username or token.
Password authentication is not supported for Git operations
```

или

```
fatal: Authentication failed for 'https://github.com/...'
```

### Причина
GitHub (и другие Git платформы) больше не поддерживают аутентификацию по паролю с августа 2021. Нужно использовать Personal Access Token (PAT) или SSH ключи.

### ✅ Решение 1: SSH ключи (рекомендуется для VPS)

```bash
# 1. Генерируем SSH ключ на Ubuntu VPS
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажимаете Enter 3 раза (путь по умолчанию, без passphrase)

# 2. Показываем публичный ключ
cat ~/.ssh/id_ed25519.pub

# 3. Копируем вывод и добавляем на GitHub:
#    - https://github.com/settings/keys
#    - "New SSH key" → вставляем ключ → "Add SSH key"

# 4. Тестируем соединение
ssh -T git@github.com
# Должно быть: "Hi USERNAME! You've successfully authenticated..."

# 5. Меняем remote URL на SSH (если репозиторий уже склонирован)
cd /path/to/your/repo
git remote set-url origin git@github.com:USERNAME/REPO.git

# 6. Теперь можно работать без пароля
git push
git pull
```

### ✅ Решение 2: Personal Access Token (для HTTPS)

```bash
# 1. Создаем токен на GitHub:
#    - https://github.com/settings/tokens
#    - "Generate new token" → "Generate new token (classic)"
#    - Выбираем срок (30 дней, 90 дней, без срока)
#    - Отмечаем "repo" для полного доступа к репозиториям
#    - "Generate token" → КОПИРУЕМ ТОКЕН (он больше не покажется!)

# 2. Настраиваем credential helper для сохранения токена
git config --global credential.helper store

# 3. При следующем git push вводим:
#    Username: ваш_github_username
#    Password: ваш_токен (НЕ пароль от аккаунта!)

# Токен сохранится в ~/.git-credentials
```

### ✅ Решение 3: Быстрый push (одноразовый)

```bash
# Если нужно быстро запушить один раз
git push https://USERNAME:TOKEN@github.com/USERNAME/REPO.git main

# Или для клонирования
git clone https://USERNAME:TOKEN@github.com/USERNAME/REPO.git
```

**Рекомендация:** Используйте SSH ключи (Решение 1) - это безопаснее и удобнее для VPS серверов.

---

## 🔴 Проблема 12: UFW блокирует соединения

### Симптомы
MCP серверы не доступны с клиента, хотя на сервере работают.

### ✅ Решение
```bash
# Проверьте статус UFW
sudo ufw status verbose

# Разрешите нужные порты
sudo ufw allow 8006/tcp
sudo ufw allow 8007/tcp

# Или разрешите с определенного IP
sudo ufw allow from YOUR_CLIENT_IP to any port 8006 proto tcp

# Проверьте снова
sudo ufw status numbered
```

---

## 📚 Полезные команды для диагностики

### Системная информация
```bash
# Версия Ubuntu
lsb_release -a
cat /etc/os-release

# Архитектура
uname -m
dpkg --print-architecture

# Ресурсы
free -h
df -h
htop
```

### Сетевая диагностика
```bash
# Открытые порты
sudo ss -tulpn
sudo netstat -tulpn  # если установлен

# Проверка доступности порта снаружи
# (с клиента)
telnet YOUR_VPS_IP 8006
nc -zv YOUR_VPS_IP 8006

# Публичный IP VPS
curl ifconfig.me
```

### Логи
```bash
# Системные логи
sudo journalctl -xe
sudo journalctl -f

# Логи конкретного сервиса
sudo journalctl -u SERVICE_NAME -f

# Логи ядра
dmesg | tail -50
```

---

## 🆘 Когда ничего не помогает

1. **Перезагрузите VPS**: `sudo reboot`
2. **Проверьте системные требования**: VPS_SETUP_GUIDE.md, раздел "Требования к VPS"
3. **Проверьте провайдера**: Убедитесь что провайдер поддерживает KVM и nested virtualization
4. **Сбросьте к чистой установке**: Переустановите Ubuntu 24.04 и начните с начала

---

**Последнее обновление:** 2026-02-02
**Применимо к:** Ubuntu 24.04 LTS

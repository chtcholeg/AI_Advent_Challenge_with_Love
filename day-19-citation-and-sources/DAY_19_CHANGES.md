# Day 19: Citation and Sources - Clickable Web URLs

## Что изменилось

Добавлена поддержка **открытия web-страниц в браузере** при клике на источники в AI Agent.

## Изменённые файлы

### 1. FileOpener (Desktop) - `ai-agent/src/desktopMain/kotlin/ru/chtcholeg/agent/util/FileOpener.desktop.kt`

**Добавлено:**
- Автоматическое определение URL (проверка префикса `http://` или `https://`)
- Открытие URL в браузере через `Desktop.getDesktop().browse(URI(path))`

```kotlin
if (path.startsWith("http://") || path.startsWith("https://")) {
    if (Desktop.isDesktopSupported()) {
        Desktop.getDesktop().browse(URI(path))
    }
} else {
    // Existing file opening logic
}
```

### 2. FileOpener (Android) - `ai-agent/src/androidMain/kotlin/ru/chtcholeg/agent/util/FileOpener.android.kt`

**Добавлено:**
- Автоматическое определение URL
- Открытие URL через Intent с `ACTION_VIEW`

```kotlin
if (path.startsWith("http://") || path.startsWith("https://")) {
    val intent = Intent(Intent.ACTION_VIEW, Uri.parse(path)).apply {
        addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
    }
    context.startActivity(intent)
} else {
    // Existing file opening logic
}
```

## Новые файлы

### 1. `CLICKABLE_SOURCES.md`
Подробная документация по использованию новой функции:
- Как индексировать web-страницы
- Как использовать в AI Agent
- Примеры
- FAQ

### 2. `demo-clickable-sources.sh`
Демонстрационный скрипт для быстрого тестирования:
- Индексирует страницу документации Kotlin
- Показывает статистику индекса
- Выполняет тестовый поиск
- Предоставляет инструкции для использования

## Как работает

### Архитектура

```
User clicks [Источник 1]
         ↓
MessageItem.kt: onSourceClick(source.filePath)
         ↓
AgentScreen.kt: FileOpener.openFile(path)
         ↓
FileOpener checks path prefix
         ↓
    ┌────────┴────────┐
    ↓                 ↓
http(s)://          file path
    ↓                 ↓
Desktop.browse()    Desktop.open()
or Intent           or FileProvider
```

### Метаданные источников

При индексировании:
- **Файлы** → `sourceType = SourceType.FILE`
- **URL** → `sourceType = SourceType.URL`

В RAG контексте:
- `SourceReference.filePath` содержит либо file path, либо URL
- FileOpener автоматически определяет тип по префиксу

## Использование

### Быстрый старт

```bash
# Запустить демо
./demo-clickable-sources.sh

# Запустить AI Agent
./gradlew :ai-agent:run
```

### Индексирование URL

```bash
# Одна страница
./gradlew :shared:runIndexing --args="indexUrl 'https://example.com' ./index.db"

# Несколько страниц
./gradlew :shared:runIndexing --args="indexUrls 'https://a.com' 'https://b.com' ./index.db"

# Через GUI
./gradlew :indexer:run
# Выбрать режим "URL" и ввести адрес
```

### В AI Agent

1. **Settings** → RAG Mode: ON
2. Указать путь к индексу с URL
3. Задать вопрос
4. **Кликнуть** на `[Источник N]` в ответе
5. Web-страница откроется в браузере

## Примеры

### Индексирование документации

```bash
# Индексировать Kotlin docs
./gradlew :shared:runIndexing --args="indexUrl 'https://kotlinlang.org/docs/coroutines-guide.html' ./kotlin.db"

# Индексировать Compose docs
./gradlew :shared:runIndexing --args="indexUrl 'https://developer.android.com/jetpack/compose' ./compose.db"
```

### Смешанные источники (файлы + URL)

```bash
# Индексировать локальные файлы
./gradlew :shared:runIndexing --args="index ./docs ./mixed.db md txt pdf"

# Добавить URL в тот же индекс
./gradlew :shared:runIndexing --args="indexUrl 'https://example.com/api' ./mixed.db"
```

При использовании:
- Локальные файлы откроются в редакторе
- URL откроются в браузере

## Технические детали

### Поддерживаемые протоколы

✅ `http://`
✅ `https://`

Для добавления других протоколов (ftp://, file://) нужно расширить проверку в FileOpener.

### Платформы

| Платформа | Механизм | Статус |
|-----------|----------|--------|
| Desktop (JVM) | `Desktop.browse()` | ✅ Реализовано |
| Android | `Intent.ACTION_VIEW` | ✅ Реализовано |

### Визуализация

Источники в AI Agent отображаются как:
- **Цвет**: синий (`#6CB6FF`)
- **Декорация**: подчёркивание
- **Шрифт**: жирный

Это делает их похожими на стандартные гиперссылки.

## Тестирование

### Unit тесты
Не требуются - простая логика проверки префикса строки.

### Ручное тестирование

1. Запустить демо-скрипт: `./demo-clickable-sources.sh`
2. Запустить AI Agent: `./gradlew :ai-agent:run`
3. Настроить RAG с demo-web-index.db
4. Задать вопрос о Kotlin coroutines
5. Кликнуть на источник
6. Проверить, что браузер открыл kotlinlang.org

### Тест на Android

1. Собрать APK: `./gradlew :ai-agent:assembleDebug`
2. Установить на устройство
3. Проиндексировать URL
4. Запустить приложение
5. Задать вопрос и кликнуть на источник
6. Проверить, что браузер Android открыл страницу

## Ограничения

1. **Только http(s)**: Другие протоколы (ftp, file) не поддерживаются
2. **Левый клик**: Средняя кнопка мыши или Ctrl+Click не поддерживаются
3. **Только AI Agent**: Chat модуль пока не поддерживает RAG

## Возможные улучшения

1. **Визуальная индикация типа источника**:
   - 🌐 для URL
   - 📄 для файлов

2. **Tooltip при наведении**:
   - Показывать полный URL/путь

3. **Контекстное меню**:
   - Правый клик → "Открыть в браузере" / "Копировать ссылку"

4. **Поддержка других протоколов**:
   - `ftp://`
   - `file://`
   - `mailto:`

5. **История кликов**:
   - Сохранять, какие источники были открыты

## Совместимость

✅ Обратная совместимость сохранена
✅ Старые индексы работают без изменений
✅ Файлы по-прежнему открываются корректно
✅ Если Desktop.browse() недоступен, ошибка игнорируется (printStackTrace)

## Коммит

```
Add clickable web URL sources in AI Agent

- FileOpener now detects URLs (http/https) and opens them in browser
- Desktop: uses Desktop.browse() with URI
- Android: uses Intent.ACTION_VIEW
- Added documentation and demo script
- Backward compatible with file sources
```

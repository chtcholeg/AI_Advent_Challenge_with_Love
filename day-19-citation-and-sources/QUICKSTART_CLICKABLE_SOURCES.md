# Quick Start: Clickable Web Sources

## Что реализовано

✅ **Клик по источнику в AI Agent теперь открывает web-страницу в браузере**

## За 3 шага

### 1. Проиндексируйте web-страницу

```bash
# Пример: индексируем Kotlin Coroutines Guide
./gradlew :shared:runIndexing --args="indexUrl 'https://kotlinlang.org/docs/coroutines-guide.html' ./web-demo.db"
```

Или используйте демо-скрипт:
```bash
./demo-clickable-sources.sh
```

### 2. Запустите AI Agent с RAG

```bash
./gradlew :ai-agent:run
```

В Settings (⚙️):
- **RAG Mode**: ON
- **Index Path**: `./web-demo.db` (или `./demo-web-index.db` если использовали скрипт)

### 3. Задайте вопрос и кликните на источник

Спросите:
```
What is a coroutine scope in Kotlin?
```

В ответе появится `[Источник 1]` - **кликните на него**, и откроется kotlinlang.org в браузере! 🎉

## Как это работает

```
[Источник 1] (клик) → FileOpener
                          ↓
            Проверка: начинается с http(s)?
                          ↓
               ┌─────────┴─────────┐
               ↓                   ↓
             Да                   Нет
               ↓                   ↓
        Открыть в браузере    Открыть файл
```

## Файлы + URL в одном индексе

```bash
# Индексировать локальные файлы
./gradlew :shared:runIndexing --args="index ./docs ./mixed.db md txt"

# Добавить URL
./gradlew :shared:runIndexing --args="indexUrl 'https://example.com' ./mixed.db"
```

Результат:
- **Локальные файлы** → откроются в редакторе
- **URL** → откроются в браузере

## Платформы

| Платформа | Как открывается |
|-----------|----------------|
| **Desktop** | Браузер по умолчанию (`Desktop.browse()`) |
| **Android** | Браузер через Intent |

## Больше информации

- 📖 [CLICKABLE_SOURCES.md](./CLICKABLE_SOURCES.md) - подробная документация
- 📋 [DAY_19_CHANGES.md](./DAY_19_CHANGES.md) - технические детали изменений

## Проблемы?

**URL не открывается?**
- Проверьте, что URL начинается с `http://` или `https://`
- Убедитесь, что Desktop.isDesktopSupported() = true (Desktop)
- Проверьте логи в консоли

**Источник не кликабелен?**
- Убедитесь, что RAG Mode включен
- Проверьте, что индекс загружен (Index Path указан правильно)
- Источники должны быть синими, подчёркнутыми и жирными

**Хотите добавить другие протоколы?**
- Расширьте проверку в `FileOpener.kt`: добавьте `path.startsWith("ftp://")` и т.д.
